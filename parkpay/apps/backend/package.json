{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "tsx watch --env-file-if-exists=.env src/server.ts",
    "dev:docker": "npm run migration:run && tsx watch src/server.ts",
    "build": "tsc",
    "start": "node dist/src/server.js",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "migration:generate": "npm run build && typeorm migration:generate src/infrastructure/persistence/migrations/InitialPayment -d dist/src/config/data-source.js",
    "migration:run": "npm run build && typeorm migration:run -d dist/src/config/data-source.js",
    "migration:revert": "npm run build && typeorm migration:revert -d dist/src/config/data-source.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "express": "^5.2.1",
    "reflect-metadata": "^0.2.2",
    "typeorm": "^1.1.1"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^9.6.0",
    "@types/express": "^5.0.6",
    "@types/node": "^26.6.2",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
