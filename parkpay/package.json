{
  "name": "parkpay",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/frontend",
    "apps/backend"
  ],
  "description": "",
  "main": "index.js",
  "scripts": {
    "lint": "oxlint .",
    "lint:fix": "oxlint . --fix",
    "test": "npm run test --workspaces --if-present",
    "build": "npm run build --workspaces --if-present"
  },
  "devDependencies": {
    "oxlint": "^1.83.0"
  }
}
